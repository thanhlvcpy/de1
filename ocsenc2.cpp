#include <bits/stdc++.h>

using namespace std;
int a,b,v;
int main(){
    cin>>a>>b>>v;
     if (V-b) mod abs(a-b) = 0 then
                writeln((v-b) div abs(a-b))
        else
                writeln(((v-b) div abs(a-b))+1);
}
